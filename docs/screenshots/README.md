# Screenshots

These images are referenced by the top-level `README.md`.

## Present (frame-annotator)
- `overview.png` — the frame-annotator clip-classification interface.
- `surgical_safety.png` — frame-annotator running the `examples/surgical_safety.yaml` config (robotic surgical-safety labelling).

## Present (surgical-annotator)
Exported from the SafeSurg abstract figures (Figures 1–3):

- `surgical_annotator_interface.png` — Figure 1: the main annotation view (two-instrument masks, shaft lines, keypoints, and the right-hand visibility / SAM / JSON panels).
- `surgical_annotator_batch.png` — Figure 2: **batch mode** — the paginated thumbnail grid with colour-coded status borders and a multi-selection.
- `surgical_annotator_video_phase.png` — Figure 3: **video mode** with the phase/event timeline showing labelled clips.

> These three are full-resolution PNGs (~1.5–3 MB each) lifted straight from the abstract. If repo size matters, they can be down-scaled to ~1400px wide without losing legibility.
